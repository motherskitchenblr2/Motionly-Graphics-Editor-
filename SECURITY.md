# Security

Report vulnerabilities privately through the repository security advisory flow.

Compositions are executable TypeScript and must be reviewed like application code. Do not run compositions from untrusted sources. Keep imported media same-origin when exporting DOM frames, and never embed provider credentials in a composition.
